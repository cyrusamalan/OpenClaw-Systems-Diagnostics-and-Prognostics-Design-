# ── Dashboard (Next.js) ────────────────────────────────────────
DASHBOARD_PORT=3000

# Internal URL used by Next.js API routes to reach the proxy
PROXY_INTERNAL_URL=http://localhost:8080

# ── Proxy Service (FastAPI) ────────────────────────────────────
PROXY_PORT=8080

# CORS origins allowed to call the proxy
CORS_ORIGINS=http://localhost:3000

# OpenClaw gateway token (from `openclaw onboard` or `openclaw config get gateway.token`)
OPENCLAW_TOKEN=5f9eeaed4ca1357f93ede2491d1d4e612917424537b130eb
